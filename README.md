# disaster-backend-api

Express + Prisma API server for SendResQPls.

## Setup

\\\ash
npm install
cp .env.example .env  # fill in your env vars
npm run dev
\\\

## Deploy

Dockerized — see \Dockerfile\. Works on Railway, Render, or any container host.
